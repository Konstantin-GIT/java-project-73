server:
  error:
    include-stacktrace: never

spring:
  devtools:
    restart:
      enabled: false
    livereload:
      enabled: false

  datasource:
    url: ${JDBC_DATABASE_URL}

#  jpa:
#    show-sql: false
#    hibernate:
#      ddl-auto: update

logging:
  level:
    ROOT: INFO
